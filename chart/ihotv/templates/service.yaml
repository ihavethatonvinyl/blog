apiVersion: v1
kind: Service
metadata:
  name: {{ include "ihotv.fullname" . }}
  labels:
    {{- include "ihotv.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ihotv.selectorLabels" . | nindent 4 }}
