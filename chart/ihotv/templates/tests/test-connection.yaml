apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ihotv.fullname" . }}-test-connection"
  labels:
    {{- include "ihotv.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ihotv.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
